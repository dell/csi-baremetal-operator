# Docker registry to pull images
global:
  registry:
  registrySecret:

# Image pull settings
image:
  name: csi-baremetal-operator
  tag: green
  pullPolicy: Always

resources:
  limits:
    cpu: 100m
    memory: 200Mi
  requests:
    cpu: 100m
    memory: 20Mi
